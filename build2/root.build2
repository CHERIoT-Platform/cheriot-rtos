#info 'loading cheriot-rtos root'

define ldscript: file
ldscript{*}: extension = ldscript

# Since we don't build anything inside the cheriot-rtos project, we only need
# the C/C++ target types.
#
using cxx.types
using c.types
using c.as-cpp.types

hxx{*}: extension = h
cxx{*}: extension = cc
